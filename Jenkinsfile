pipeline {
   agent any
      stages {
         stage('Preparing environment') {
         }
         stage('Clone SCM') {
         }
         stage('Build') {
         }
         stage('Unit Tests') {
            steps {
               echo 'Unit Tests'
            }
         }
         stage('Integration Tests') {
            steps {
               echo 'Integration Tests'
            }
         }
         stage('Sending results') {
         }
      }
}
